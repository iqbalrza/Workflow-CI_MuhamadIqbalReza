name: telco_churn_msml

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      n_estimators:
        type: int
        default: 200
      max_depth:
        type: str
        default: "10"
    command: >
      python modelling.py